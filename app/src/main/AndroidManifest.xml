<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ram.munindia">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:configChanges="keyboard|keyboardHidden|orientation"
        android:icon="@drawable/mun"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mun"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Dispatch"/>
        <activity android:name=".DispatchList"/>
        <activity android:name=".GRNapproval" />
        <activity android:name=".DispencerList" />
        <activity android:name=".FgLocation" />
        <activity android:name=".ScanInput" />
        <activity android:name=".RmLocation" />
        <activity android:name=".BtWeight" />
        <activity android:name=".Login" />
        <activity android:name=".Dashboard" />
        <activity android:name=".Barcodescanner" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PrintBt.DeviceListActivity"
            android:theme="@android:style/Theme.DeviceDefault.Dialog" />
    </application>

</manifest>